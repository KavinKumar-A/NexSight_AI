# ════════════════════════════════════════════════════
# NexSight AI — Requirements
# Microsoft Build AI Hackathon 2025
# Tested on Python 3.12 (lower-bound pins use prebuilt wheels).
# ════════════════════════════════════════════════════

# ── Web Framework ──────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
aiofiles>=24.1

# ── Data Processing ────────────────────────────────
pandas>=2.2
numpy>=1.26
scipy>=1.13

# ── Machine Learning ───────────────────────────────
scikit-learn>=1.5
xgboost>=2.1
lightgbm>=4.4

# ── Computer Vision ────────────────────────────────
Pillow>=10.4
opencv-python-headless>=4.10

# ── Visualization ──────────────────────────────────
plotly>=5.22

# ── Utilities ──────────────────────────────────────
joblib>=1.4
tqdm>=4.66
pydantic>=2.8
httpx>=0.27

# ── Optional (Explainable AI) ──────────────────────
# SHAP enriches root-cause analysis; the engine falls back to
# permutation importance when it is not installed. Install with:
#   pip install shap
# shap>=0.45
